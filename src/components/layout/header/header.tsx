import { MainHeader } from './mainHeader';
import { TopBarHeader } from './topBarHeader';
export function Header() {
  return (
    <header>
      <TopBarHeader />
      <MainHeader />
    </header>
  );
}
